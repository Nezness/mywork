# Day6logs
### About $PATH
# ex)echo $PATH
# /usr/local/sbin:/usr/local/bin:...:/usr/sbin:/usr/bin:/sbin:/bin
# Type ls
- looking for ls /usr/local/sbin
- looking for ls /usr/local/bin
- ...
- /usr/bin/ls ... ls is found and execute it.
# Where is ls?
- which ls
- /user/bin/ls
# What does type tell?
- type ls
- ls is aliased to `ls --color=auto'
- type cd
- cd is a shell builtin

# How visible can be repository
- sudo apt install tree
- tree -a
- echo "thumbs.db" > .gitignore
- echo ".DS_Store" >> .gitignore
- cat << 'EOF' >> .gitignore
- .vscode
- .idea/
- *.swp
- *.tmp
- *.bak
- *.old
- EOF

# ;(always execute)
# &&(success then execute)
# ||(fault then execute)
- mkdir test; ls test; rmdir test
- mkdir test; cd test || rmdir test
- cd test || echo "cd was not founded! Could you make test dir?"
- cd /var/log && ls -l || echo "access denied" >> error.log

# What is "exit code($?)" ?
- echo $?
# This refer to result before
# success = 0, failed = 1~255
- ls /notfound
- echo $?
- 2

# 1= common error
# 2= shell error
# 126= couldn't execute
# 127= command is not found
# 130= Ctrl + C

## && and || is like filter
## &&(0= PASS)
## ||(1~255= PASS)
